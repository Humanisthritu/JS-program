//Question : Write a program that calculates the ticket price based on age.

let personAge = 12
let tPrice;

const price = (age , ticketPrice) => {
    if(age <= 7){
        console.log(` Rs. ${ticketPrice = 10}`)
       }
       else if(age > 7 && age <= 30 ){
           console.log(` Rs. ${ticketPrice = 70}`)
       }
       else{
           console.log(` Rs. ${ticketPrice = 30}`)
       }
       
}

price(personAge ,tPrice)
